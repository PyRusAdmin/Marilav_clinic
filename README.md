# Telegram-бот для анонимных вопросов "МариЛав"

Бот для приема анонимных вопросов от пользователей, модерации администратором и публикации ответов в виде видеосообщений
в Telegram-канале клиники.

## Особенности

- ✅ Полная анонимность пользователей
- ✅ Модерация вопросов администратором
- ✅ Ответы в формате видеосообщений (кружочков)
- ✅ Автоматическая публикация в Telegram-канале
- ✅ Логирование всех действий
- ✅ Валидация вопросов (максимум 1000 символов)

## Технологии

- **Python 3.10+**
- **aiogram 3** - фреймворк для Telegram ботов
- **Peewee** - ORM для работы с базой данных
- **SQLite** - база данных
- **python-dotenv** - управление конфигурацией

## Структура проекта

```
telegram_bot/
├── bot.py              # Основной файл бота с обработчиками
├── models.py           # Модели базы данных
├── config.py           # Конфигурация
├── utils.py            # Вспомогательные функции
├── requirements.txt    # Зависимости Python
├── .env.example        # Пример конфигурационного файла
├── .gitignore         # Исключения для git
└── README.md          # Документация
```

## Установка и настройка

### 1. Клонирование проекта

```bash
cd telegram_bot
```

### 2. Создание виртуального окружения

```bash
python3 -m venv venv
source venv/bin/activate  # Для Linux/macOS
# или
venv\Scripts\activate  # Для Windows
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка конфигурации

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Откройте `.env` и заполните параметры:

```env
BOT_TOKEN=your_bot_token_here
ADMIN_ID=123456789
CHANNEL_ID=@your_channel
```

#### Как получить необходимые данные:

1. **BOT_TOKEN**:
    - Напишите [@BotFather](https://t.me/BotFather) в Telegram
    - Отправьте команду `/newbot`
    - Следуйте инструкциям и получите токен

2. **ADMIN_ID**:
    - Напишите [@userinfobot](https://t.me/userinfobot) в Telegram
    - Бот пришлет вам ваш ID

3. **CHANNEL_ID**:
    - Создайте публичный канал (например: @marilove_channel)
    - Или используйте числовой ID приватного канала (например: -1001234567890)
    - **Важно**: Добавьте бота в канал как администратора с правами на публикацию

### 5. Запуск бота

```bash
python bot.py
```

При первом запуске автоматически создастся база данных `questions.db`.

## Использование

### Для пользователей (пациентов)

1. Найдите бота в Telegram
2. Нажмите `/start` или кнопку "Начать"
3. Напишите свой вопрос (до 1000 символов)
4. Получите подтверждение отправки
5. Ответ будет опубликован в канале клиники

**Примечания:**

- Вопросы полностью анонимны
- Нельзя отправлять фото, видео или другие файлы
- Только текстовые вопросы

### Для администратора

1. Получите уведомление о новом вопросе
2. Увидите текст вопроса и две кнопки:
    - ✅ **Принять** - для публикации
    - ❌ **Отклонить** - для отклонения

3. При принятии вопроса:
    - Бот попросит отправить видеосообщение (кружочек)
    - Запишите и отправьте ответ
    - Бот автоматически опубликует вопрос и ответ в канале

4. При отклонении:
    - Вопрос будет отмечен как отклоненный
    - Публикации не будет

## Формат публикации в канале

Пост в канале будет выглядеть так:

```
[Видеосообщение с ответом]

❓ Вопрос: [текст вопроса пользователя]

📍 На вопросы отвечают квалифицированные врачи: косметологи, массажисты, 
специалисты по коррекции фигуры и главный врач клиники Мария Лаврентьева. 
Ответ может занять какое-то время.

👉 Подписывайтесь: @marilove_channel
```

## База данных

Структура таблицы `questions`:

| Поле          | Тип                   | Описание                                |
|---------------|-----------------------|-----------------------------------------|
| id            | VARCHAR (PRIMARY KEY) | Уникальный идентификатор вопроса (UUID) |
| text          | TEXT                  | Текст вопроса                           |
| status        | VARCHAR               | Статус: pending, approved, rejected     |
| video_file_id | VARCHAR               | ID видеосообщения в Telegram            |
| created_at    | DATETIME              | Дата и время создания                   |

## Логирование

Все действия бота записываются в файл `bot.log`:

- Отправка вопросов пользователями
- Действия администратора (принятие/отклонение)
- Публикации в канале
- Ошибки

## Безопасность

✅ Полная анонимность пользователей - никакие данные о них не сохраняются  
✅ В базе данных и интерфейсе администратора нет информации об отправителях  
✅ Только администратор может модерировать вопросы  
✅ Токены и ID хранятся в `.env` файле (не в репозитории)

## Резервное копирование

Рекомендуется регулярно создавать резервные копии базы данных:

```bash
# Создание бэкапа
cp questions.db questions_backup_$(date +%Y%m%d).db

# Или автоматизированное резервное копирование через cron
0 2 * * * cp /path/to/questions.db /path/to/backups/questions_$(date +\%Y\%m\%d).db
```

## Возможные проблемы и решения

### Бот не отвечает

- Проверьте правильность токена в `.env`
- Убедитесь, что бот запущен
- Проверьте логи в `bot.log`

### Не приходят уведомления администратору

- Проверьте правильность ADMIN_ID в `.env`
- Убедитесь, что вы написали боту хотя бы один раз

### Не публикуется в канале

- Убедитесь, что бот добавлен в канал как администратор
- Проверьте правильность CHANNEL_ID в `.env`
- Для приватных каналов используйте числовой ID (начинается с -100)

### База данных не создается

- Убедитесь, что у программы есть права на запись в директорию
- Проверьте, что не запущено несколько экземпляров бота

## Масштабирование

### Несколько администраторов

Для добавления нескольких администраторов измените `config.py`:

```python
# В config.py
ADMIN_IDS = [int(id.strip()) for id in os.getenv('ADMIN_IDS', '').split(',')]

# В .env
ADMIN_IDS = 123456789, 987654321, 555666777
```

Затем в `bot.py` измените проверки:

```python
if message.from_user.id in ADMIN_IDS:
# ...
```

## Поддержка

При возникновении проблем:

1. Проверьте файл `bot.log`
2. Убедитесь в правильности конфигурации `.env`
3. Проверьте, что все зависимости установлены
4. Убедитесь, что используется Python 3.10+

## Лицензия

Проект создан для клиники "МариЛав" по техническому заданию.

---

**Разработано с использованием:**

- aiogram 3
- Peewee ORM
- SQLite
